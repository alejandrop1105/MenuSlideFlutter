name: menu_slide_flutter
description: >-
  A reusable, themeable Flutter side-menu shell with a diagonal reveal
  animation. Zero third-party runtime dependencies.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.7.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
